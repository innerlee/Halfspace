export
    boostNet  # BoostNet

""" BoostNet """
function boostNet(features, labels; m=2, delta=.5, gamma=1, T=1, B=10)
  d, n = size(features) # feature dim
  # f0 = 0, b[0] = 0
  
  # compute g_t by alg3
  
  # set f_t = f_t-1 + ..., b_t = ...
  
  say("love from 4")
end
