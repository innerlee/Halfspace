export
    alg3  # Algorithm for learning neural networks

""" Algorithm for learning neural networks """
function alg3(features, labels; m, k, s, T, B, weight)
  m <: Int && m > 0 || error("`m` cannot be `$m`")
  # sample k points w.r.t. weight
  
  if m == 1
  
  else
  
  end

  say("love from 3")
end
